開発で使う主要コマンド:
- `pnpm dev` : 開発サーバー起動
- `pnpm build-content` : Contentlayerでコンテンツ処理と型生成
- `pnpm build` : 本番ビルド + `next-sitemap` 実行
- `pnpm start` : 本番サーバー起動
- `pnpm lint` : ESLint実行
- `npx playwright test` : E2Eテスト実行
補助コマンド: `rg` で高速検索、`ls`, `find`, `git status`, `git diff` など。macOS環境。